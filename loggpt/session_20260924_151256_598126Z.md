# GPT-20B handoff — 2026-09-24T15:12:56.598126+00:00

Branch: main
Base commit: 0224a500cd27d9c99f02d503e1156a87bca1d11b

# GPT-20B handoff — 2026-09-25 (spawn-chain research)

Branch: main
Base commit: 0224a500cd27d9c99f02d503e1156a87bca1d11b

# GPT-20B session log

## 1. Task and times

- Task: continue from `0224a50` — trace map ID → spawn → ModelID →
  objectDataId → verified OBJ/IMG: record exact packet fields/order/ID types/
  code locations in Original 1.17 + PC JAR readers; bounded search TTpc and
  revived APK for real spawn records or ModelID→objectDataId; compare with
  BLRM spawn response and identify the first missing/incorrect field; produce
  a small reviewable proposal (exact server file/handler, required packet
  change, one VERIFIED example entity with map+position, or BLOCKED with the
  precise capture needed). Write under `docs/research/`, log this file, run
  `99_agent_handoff.bat` then `99_gpt20_publish.bat`; commits start with
  `gpt20:`.
- Start: 2026-09-25 session after base `0224a50` (tree clean).
- End: this log, immediately before handoff/publish BATs.
- Constraints: no server modification; no OCR automation; no recursive TTpc
  scan; bounded jar string search only.

## 2. Base commit and files changed

- Base: `0224a500cd27d9c99f02d503e1156a87bca1d11b` (`gpt20: publish session 2026-09-24 14:44 UTC`), branch `main`, clean at start.
- Files changed (not yet committed until publisher):
  - **New** `docs/research/MAP_SPAWN_OBJECT_CHAIN_PROPOSAL.md` — full field tables, joins, comparison, proposal.
  - **Edited** `docs/RE_FINDINGS.md` — spawn-chain section.
  - **Edited** `PROJECT_STATE.md` — research blurb.
  - **New** this file `loggpt/_current.md`.
- Temp (outside repo): `C:\Users\Mech\AppData\Local\Temp\opencode\scan_spawn.py`, `pc_sprite_view.txt`.
- **Not modified:** server source, TTpc, both APKs, catalog artifacts.

## 3. Source evidence (paths / methods / status)

### Original Android 1.17 (JADX `build/research/jadx/sources/com/t4game`)

| Evidence | Location | Status |
|---|---|---|
| cmd132 = `SPRITE_VIEW_MESSAGE` | `MessageCommands.java:594`; dispatch `GameWorld.java:7299` | VERIFIED |
| `processViewSpriteMessage` header H1–H5 + GNPC N1–N20 | `GameWorld.java:12681–12782` | VERIFIED (order/types) |
| `readSpriteView` block C (canSelect…name) | `:13234–13246` | VERIFIED |
| `ReadNpcFlagFuction` count≤0→null | `:14100` | VERIFIED |
| `readSpriteView1` block V (imageCount…fabao) | `:13249–13284` | VERIFIED |
| cmd9 = `NPC_LEAD_LIST_MESSAGE`; mode0 id i32/name str/X-Y i16 | `MessageCommands.java:385`; dispatch `:7014`; body `:8800–8844` | VERIFIED |
| cmd323 collection view; objectDataId i16 | `MessageCommands.java:96`; dispatch `:7772`; body `:4186–4226` | VERIFIED |
| objectDataId → `/data/obj/<id>.obj` | `GSprite.setObjectDataId:1376` → `GDataManager.getObjectData(DATATYPE_OBJECT,…)` → `GBaseData.initFromJar` | VERIFIED |
| OBJ embeds IMG slots | `GObjectData.init:297–310` | VERIFIED |
| packet image_ids override then fallback to obj slots | `GSprite.initImageDataList:829`; `GObjectFrameData.draw:33–36` | VERIFIED |
| draw requires ready objectData | `GSprite.draw:476` | VERIFIED |
| setPos before setObjectDataId; worldX2Scene identity | `:12780–12781`; `GMap.java:81,85` | VERIFIED |
| ModelID on Original cmd132 | absent from DEX/path | REJECTED as wire field |

### PC jar

| Evidence | Location | Status |
|---|---|---|
| javap `main.b.a.g.g` public methods; `a(DataInputStream, short)` reads int id + UTF×3 then zs language `y.b/c/e` | `C:\Users\Mech\AppData\Local\Temp\opencode\pc_sprite_view.txt` (javap -c) | PARTIAL / CANDIDATE — different framing from Original |
| prior bounded class-entry string scan spawn/ModelID/n420/… | MATCHED_ENTRIES=0 | NEGATIVE (bounded) |

### Client-file spawn search

| Tree | Result | Status |
|---|---|---|
| TTpc `assets/conf` needles spawn/出生/刷怪/ModelID/n420/m510 | no spawn table | UNKNOWN for spawn XY |
| Revived APK 42 conf/json/txt/csv/properties entries, same needles | hits 0 | UNKNOWN for spawn XY / ModelID→obj |
| Revived `map_id_remap.json` | map remap only | VERIFIED remap; no spawn |

### BLRM server (read-only)

| Item | Location | Status |
|---|---|---|
| `npc_view_body` GNPC encoder | `server/truthan_local_server_v026.py:656–734` | layout matches DEX PARTIAL overall |
| probe population | `:744–842`; gates `:266–268` | CANDIDATE probes only |
| cmd9 emission | none in file | MISSING |
| Example entity TEST OBJ1014 scene9068 (203,253) obj1014 | probe + run112928 | VERIFIED narrow runtime; not original tutorial |

### First missing/incorrect field (comparison result)

1. **objectDataId** for real tutorial NPC/mob — UNKNOWN (first missing identity).
2. **spawn X/Y** for those entities — UNKNOWN (first missing position).
3. **cmd=9 nav list** never sent — first missing companion packet.
4. cmd132 field **order is not incorrect** in `npc_view_body`.
5. ModelID is not a required Original wire field.

### VERIFIED example entity

- map/scn `9068`, player (180,230); diagnostic sprite 220010 `TEST OBJ1014`,
  objectDataId 1014, pos (203,253) — run112928. Not 彩云仙子 (identity UNKNOWN).

### Proposal (no code written)

- Do not change `npc_view_body` order.
- When original objectDataId/XY evidence exists: fill
  `starter_probe_entities` / population path; add cmd9 encoder
  (`mode=0`, count, i32 id, str name, i16 X/Y, flags) after cmd132.
- Precise BLOCKED capture: original **cmd=132 GNPC type=2** body on tutorial
  scene after enter-scene, or **cmd=9 mode0** list for that scene, or any
  packet carrying both ModelID and objectDataId (none in Original DEX).

## 4. Commands run and results

- `git status` / `git log --oneline -5` — clean, base `0224a50`.
- Grep/read JADX GameWorld / MessageCommands / GSprite / GObjectData /
  GNPC / GObjectFrameData / PROTOCOL.md / MODELID_MAP_SPAWN_JOIN.md.
- `javap -classpath ZhuShen-1.0.0.jar -p -c main.b.a.g.g` → dump
  `pc_sprite_view.txt`; located `a(DataInputStream, short)` and
  `b(DataInputStream, boolean)` (UTF ModelID-first readers).
- PowerShell scan of TTpc `assets/conf` (all conf files) — no spawn hits.
- Python zipfile scan of revived APK conf/json/txt/csv/properties — hits 0.
- **Not run:** emulator launch, `01_test_capture.bat`, real-client spawn test
  (static research session only). **No server edit.**

## 5. Blockers and next task for Codex

- Blockers: original objectDataId + spawn XY UNKNOWN; ModelID→objectDataId
  UNKNOWN; cmd9 encoder not implemented; BOOTSTRAP real-client PASS not
  recorded (unchanged).
- Exact next task: when Codex has a real cmd132/cmd9 capture or server dump of
  tutorial NPC objectDataId+XY, implement data fill + optional cmd9 encoder in
  `truthan_local_server_v026.py` per proposal §6, then run
  `01_test_capture.bat`. Until then, keep probes gated and do not rename
  OBJ probes to tutorial names.
