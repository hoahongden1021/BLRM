# GPT-20B handoff — 2026-09-25T10:35:59.633337+00:00

Branch: main
Base commit: 90bbe3cd89d20bf948103985979d6a576ee19eb5

# Screen watcher repair — 2026-09-25

Base commit: `90bbe3c` (latest published commit at task start).

## Changes

- `tools/truthan_screen_watch.py`: detached/no-window worker; continuous decode independent of OCR; separate frame/OCR IDs, timestamps and ages; post-request `observe`; bounded stream reconnect; atomic status-file writes now retry transient Windows locks.
- `tools/truthan_ui_control.py`: added `observe --source auto|watcher|adb`; watcher OCR is used only after a post-request frame, screenshot/device dimensions are checked before mapping, stale observations are refused before taps, and unavailable watcher errors use fresh ADB screenshot/OCR.
- Existing bootstrap and movement paths were preserved. New tests cover stale watcher ages and dimension mismatch.

## Evidence

- Fresh runtime: `py tools\truthan_gui.py launch` succeeded; server PID 14976, five reverse ports present, client launched.
- `py tools\truthan_screen_watch.py start` returned in 4.41 seconds in the invoking PowerShell. A detached-parent test in the preceding run exited its parent PowerShell after 7.68 seconds while the watcher remained live with `MainWindowHandle=0`.
- Deliberately interrupted stream recovered after one bounded reconnect; device scrcpy exit code 143 was logged and frame ID advanced 230→357.
- Safe watcher observation: START_MENU, frame age 169.8 ms, OCR age 7.7 ms, request-to-OCR 2317.1 ms, 2992×1344. End-to-end 6.146 seconds.
- Independent fresh ADB screenshot/OCR agreed on START_MENU, 2992×1344 and the visible start/repair/exit labels; end-to-end 7.082 seconds. No taps were made.
- Final soak: 2026-09-25 10:18:26–10:28:37 UTC, 611.2 seconds, 21 timestamped samples. Frame IDs 1779→9708, minimum 382 new frames/interval, maximum frame age 301.4 ms. OCR age 51.8–3460.5 ms. CPU +340.2 seconds, max 26.6 seconds/interval; 50–54 threads; private memory 626.0–728.3 MB (728.3 first, 659.5 last). All samples running, zero reconnects/errors.
- `py -m py_compile tools\truthan_screen_watch.py tools\truthan_ui_control.py tests\test_truthan_ui_control.py`: passed.
- `py -m unittest tests.test_truthan_ui_control -v`: 11 tests passed.
- Final `py tools\truthan_screen_watch.py stop`: worker PID 23692 stopped without force.
- Full timeline and captured metrics: `docs/research/SCREEN_WATCH_REPAIR_20260925.md`, `docs/research/SCREEN_WATCH_SOAK_20260925.json`, and `docs/research/SCREEN_WATCH_SOAK_20260925_summary.json`.

## Failures and limits

- The first capped-thread soak exposed a transient `PermissionError` replacing `status.json`, which caused an unnecessary reconnect. PID/thread-specific temporary names and bounded replace retries fixed it; the post-fix soak had no errors or reconnects.
- First sampler launch was blocked by PowerShell execution policy; process-scoped `-ExecutionPolicy Bypass` started the same local script without changing system policy.
- OCR age peaked at 3.46 seconds despite sub-302 ms frame age. The watcher status does not imply fresh OCR; UI taps require age limits and the controller retains ADB OCR fallback.
- Existing broader blocker remains the unrelated post-auth port-19000 peer EOF before bootstrap reaches role list/IN_GAME; see `PROJECT_STATE.md`.

## Final commands

`99_agent_handoff.bat` and `99_gpt20_publish.bat --retry` are run after this handoff note. Their complete direct output is returned in the task response; `PUSH_OK` is reported only if the publisher prints it.
