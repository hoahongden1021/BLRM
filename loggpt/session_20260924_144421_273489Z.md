# GPT-20B handoff — 2026-09-24T14:44:21.273489+00:00

Branch: main
Base commit: 8451e618240269bea2902118748190e0d044838d

# GPT-20B handoff — 2026-09-24T14:05:00+00:00

Branch: main
Base commit: 8451e618240269bea2902118748190e0d044838d

# GPT-20B session log

## 1. Task and times

- Task: continue BLRM from base `8451e61` — build a visual, source-backed,
  locally openable HTML game catalog (skills, weapons, NPCs, monsters, mounts,
  magic items) under `docs/research/`, mapping entity IDs/iconIds/modelIds to
  image resources with exact code/data evidence across PC JAR + both APKs;
  log this file; publish via BAT scripts. Commits must start with `gpt20:`.
- Start: 2026-09-24 after previous publish `8451e618240269bea2902118748190e0d044838d`.
- End: this log, immediately before `99_agent_handoff.bat` / `99_gpt20_publish.bat`.

## 2. Base commit and files changed

- Base: `8451e618240269bea2902118748190e0d044838d` (`gpt20: publish session 2026-09-24 13:45 UTC`), branch `main`, tree clean at start.
- Files changed (not yet committed until publisher):
  - **New** `docs/research/ENTITY_IMAGE_CATALOG.md` — counts, artifacts, unresolved joins.
  - **New** `docs/research/entity_image_catalog.html` — local visual catalog (relative previews).
  - **New** `docs/research/catalog_resolution_rules.csv` — ID→image rules with status.
  - **New** `docs/research/catalog_objects_images.csv` — all 377 obj→img chains.
  - **New** `docs/research/catalog_npc_names.csv` (512), `catalog_monster_names.csv` (624), `catalog_mount_names.csv` (108), `catalog_weapon_names.csv` (720), `catalog_fabao_names.csv` (30).
  - **New** `docs/research/catalog_skills.csv` (384 DESCRIPTION_ONLY + packet icon rule).
  - **New** `docs/research/catalog_pgg_groups.csv`, `catalog_unmatched_images.csv`, `catalog_prop_pngs.csv`, `catalog_counts.json`.
  - **New** `docs/research/catalog_previews/` — PNGs extracted from linked `.img` files (~345 files).
  - **Edited** `docs/RE_FINDINGS.md` — entity↔image catalog section.
  - **Edited** `PROJECT_STATE.md` — research blurb.
  - **Edited** `docs/ENTITIES.md` — catalog pointer.
  - **New** this file `loggpt/_current.md` (publisher rotates to session log).
- Temp working files (outside repo): `C:\Users\Mech\AppData\Local\Temp\opencode\build_entity_catalog.py`, `scan_prop_icons.py`, `conf_meta.py`.
- **Not modified:** server source, TTpc tree, BLRM-local-inputs, original APK, protocol/spawn IDs.

## 3. Evidence (paths / methods / status)

| Finding | Evidence | Status |
|---|---|---|
| objectDataId → `res/data/obj/{id}.obj` → image-slot shorts → `res/data/img/{imgId}.img` | `GObjectData.init` (Original JADX); PC `main.b.a.e.a.d` `[APK_NPC]`/`[APK_CREATURE]`; offline parse of all 377 objs | VERIFIED |
| 377 objs, 295 with ≥1 image slot, 303 images referenced, 1724 unmatched | generated `catalog_counts.json` from TTpc `res/data` | VERIFIED |
| Goods icon = packet `iconId` + `type` → pgg `GOOGS_PNG[type]` index `iconId-type*1000` | `CommonConstants.GOOGS_PNG={0,1,2,2,2,5,6,7}`; `GameWorld.getGoodsIcon`; `GPNGGroupData.getIconIdList`; `GDataManager.MAX_M=1000` | VERIFIED |
| Skill icon = packet `RoleSkill.iconId` → pgg group **5** | `RoleSkillList.setIconList`; `GameWorld.java:16761`; PC `main.b.a.f.A` logs `skill id / png id` from `M.s` | VERIFIED |
| Static skill id → iconId table | none in `zsSkills_language.dat` (no icon column) or DEX/jar strings | UNKNOWN |
| Static item id → iconId table | none in equipment name tables (name/desc only) | UNKNOWN |
| NPC/monster/mount ModelID → objectDataId | name tables only; no obj column (`MODELID_MAP_SPAWN_JOIN.md`) | UNKNOWN |
| `prop/{id}.png` entity key | ImageManager path category `prop` builds `/{dir}/{id}.png`; no caller proven to item id | UNKNOWN |
| `anuId≥9000` skill effect | `apk_skill_effects.json` load_convention (ClientZS note) | CANDIDATE |
| Path categories in PC ImageManager | `main.b.a.e.k` static array: prop/prop_v5/sys/ingame/ui/skill/npc/orge/fabao/weapon/… | VERIFIED (categories) |
| AES conf tables | key `1234567812345678` AES/ECB/PKCS5Padding in `main.b.a.a.r` | VERIFIED |

## 4. Commands run and real results

- Mandatory startup reads: AGENTS.md (given), AI_RULES.md, LOCAL_AGENT_RULES.md, PROJECT_STATE.md, relevant `docs/` — done; `git status` clean; `git log` base `8451e61`.
- `javap` on PC jar: `main.b.a.e.k` (ImageManager path build), `main.b.a.f.A`/`M`/`C` (skill png id), `main.b.a.e.a.d` (APK_NPC/CREATURE), language loaders `y`/`v`/`w`/`p`.
- String scan of jar classes for prop/pgg/GOODS/skill id markers.
- JADX reads: `GPNGGroupData`, `CommonConstants.GOOGS_PNG`, `GDataManager`, `RoleSkill`/`RoleSkillList`, `GameWorld.getGoodsIcon`.
- AES-decoded conf tables (temp `conf_meta.py` + `build_entity_catalog.py`).
- Generated catalog CSVs + HTML + extracted PNG previews from `.img` files.
- **Not run:** emulator/runtime game test, `01_test_capture.bat` (static research only; BOOTSTRAP phase gate N/A).
- Publisher not yet run when this paragraph was written; see bottom of publish BAT output for PUSH_OK.

## 5. Blockers and exact next task

**Blockers:**

1. skill id → iconId static mapping UNKNOWN (packet only).
2. item id → iconId static mapping UNKNOWN (packet only).
3. ModelID → objectDataId still UNKNOWN (needs cmd9/cmd132 capture or server dump).
4. `prop/{id}.png` key formation not proven to any entity id.
5. BOOTSTRAP real-client PASS still not recorded (unchanged; this session was static research only).

**Next recommended action:**

- Capture real-client goods/skill detail packets (BOOTSTRAP → login → open inventory/skill UI) to record concrete `iconId` samples; OR find server-side goods config with an icon column. Do not invent ModelID→obj or visual identity joins.

**Current blocker for automation phases:** BOOTSTRAP real-client PASS still not recorded (unchanged; this session was static research only).
