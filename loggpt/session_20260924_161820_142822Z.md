# GPT-20B handoff — 2026-09-24T16:18:20.142822+00:00

Branch: main
Base commit: e0a26baf1ceafaccfdf441109a5350a2e07d9226

# Current session handoff — 2026-09-25

## Task list

1. Fix duplicate `edit` keys in `opencode.json` preserving TTpc/`BLRM-local-inputs` deny — **DONE** (`NO_DUP_KEYS_OK`, write unchanged).
2. Integration check of real server map-entry send path (`TRUTHAN_DATA_SPAWN=1` → cmd132 for TEST OBJ1014 and RECONSTRUCTED OBJ1155; off → none; login/movement keep working) — **DONE** (`tests/test_map_entry_integration.py`, suite **23 OK**).
3. Trace real `.scn` decoder, decode `9068.scn`, document fields + walkability, determine (not assume) entity positions — **DONE for parse/walkability** (`docs/MAPS.md`); back layer residual 299 B unparsed; original spawn XY remains UNKNOWN (no spawn table in `.scn`).
4. Extend fixture with one hittable TEST monster only if assets/fields/position verified — **DONE**: OBJ1155/IMG1207 VERIFIED, `can_hit=1`, position (196,238) walkable, label `RECONSTRUCTED`, never claimed as original spawn.
5. Diagnose `peer_eof` + noninteractive bootstrap with character-list handling — **PARTIAL**: auth cmd42/−42 **VERIFIED** on real Android client (packet log `20260925_005625_p29000`, layout matches `processLoginMessage`/`processServerList`); post-auth client opens 19000 then **immediately peer_eof, zero cmd277**, UI stuck NETWORK_ERROR `网络错误` + `读取中...100%` (State 100 / `tcpState` not OPEN). Cause **UNKNOWN**. Never reached server list, role list, or IN_GAME.

## Current blocker

Real-client map-entry / visual PASS not achieved. Bootstrap progressed:
START_MENU → ACCOUNT_LOGIN → auth cmd42/−42 OK → **NETWORK_ERROR before
server-list / role-list** (19000 open then peer_eof with zero frames;
loading dialog stuck at 100%). Character-list handling not yet reached.
Failure at this stage ≠ spawn failure; report exact screen + packet stage.

**Plainly: no visual PASS — the Android client never rendered entities
this session.** The Python integration-test 19000 flows (cmd277→20→6→7→32→10→132)
are **not** the Android client; they only prove the cmd132 encoder/fixtures.

## Next recommended action

1. Diagnose post-auth failure: read `TcpNetwork` ctor `:117`/`open()` and
   `getAuthUrl`/`isHttp` defaults; check logcat for `TCP重连接`/`tcp连接地址`
   prints; optionally re-run login and capture whether a server-list screen
   flashes before State 100. If unresolvable within bounds, keep task 5 PARTIAL
   with exact stage: "auth cmd42/−42 OK → client opens 19000 → zero frames →
   NETWORK_ERROR 网络错误/读取中".
2. If 19000 handshake succeeds: handle ROLE_LIST (select existing or create
   ONE test role once) → scene 9068 → IN_GAME proof (`game_connected=true` +
   scene id + x/y) + observe TEST OBJ1014 (and optionally OBJ1155).
3. If stuck: capture screenshot + OCR JSON + packet log + `runtime_state.json`,
   report stage (login/role/selection/loading/scene).
4. Publish: this file + `docs/*` updated, then `99_agent_handoff.bat` then
   `99_gpt20_publish.bat`.

## Evidence already in repo / docs

- `docs/MAPS.md` — full 9068.scn decode + walkability.
- `docs/ENTITIES.md` — OBJ1155 RECONSTRUCTED monster fixture.
- `docs/RE_FINDINGS.md` — decode summary, integration cmd7, peer_eof logs,
  **this session's auth + post-auth bootstrap section**.
- `docs/PROTOCOL.md` — auth cmd42/−42 layout + post-auth blocker.
- `docs/AUTOMATION_BOOTSTRAP.md` — session OCR screens, tap coords,
  focus-then-activate, bootstrap_step caveat, NETWORK_ERROR stuck-at-100%.
- `tests/test_map_entry_integration.py` — 23 OK with monster.
- `server/truthan_local_server_v026.py` — monster fixture + Unicode-safe `send()` label print.
- Packet logs: `server/truthan_packet_logs/20260925_005625_p29000_...` (auth frames),
  `20260925_005839_p19000` (empty 19000 open/close).

## Unresolved (do not guess)

- Original tutorial objectDataId / spawn XY / 火狐妖↔OBJ mapping.
- Back-layer 299 B parse.
- Cause of post-auth NETWORK_ERROR / State 100 (TcpNetwork open / isHttp / tcpState).
- Client-side visual confirmation of fixtures (no PASS yet).

## Commands run (real results)

- `py -m pytest tests/ -q` → **23 passed** (baseline 21 + integration 2).
- `py tools\truthan_gui.py launch` with `TRUTHAN_DATA_SPAWN=1` + `TRUTHAN_SCENE=9068` → server fresh, all ports, reverse rules OK; `runtime_state` scene 9068.
- OCR + double-tap flow: START_MENU → ACCOUNT_LOGIN → auth frames captured.
- Post-auth: `读取中` 0→100% then stuck; 确定 tap did not dismiss; no cmd277 on 19000.
- `git status`: modified `PROJECT_STATE.md`, `docs/ENTITIES.md`, `docs/MAPS.md`,
  `docs/RE_FINDINGS.md`, `docs/PROTOCOL.md`, `docs/AUTOMATION_BOOTSTRAP.md`,
  `opencode.json`, `server/truthan_local_server_v026.py`; new
  `tests/test_map_entry_integration.py`, `loggpt/_current.md`.
