# GPT-20B handoff — 2026-09-24T15:32:34.704673+00:00

Branch: main
Base commit: cb7a651706a79e27e666ab1d77997e1ba5156bab

# GPT-20B session log — 2026-09-25 (cmd9 callers + data-driven spawn)

Branch: main
Base commit: cb7a651706a79e27e666ab1d77997e1ba5156bab
Start (local): ~2026-09-25T00:00+09:00 (session after base clean)
End: immediately before handoff/publish BATs, 2026-09-25 ~00:31+09:00

## 1. Task

Continue from `cb7a651`:

1. Trace cmd9 callers — required for sprite visibility or only NPC guide/list UI?
2. Implement a small data-driven spawn path behind an explicit switch; TEST OBJ1014 fixture first; preserve login/map/movement; keep TEST label.
3. If a controlled revived-client session is available, passively record one map-entry/entity sequence (no tap automation, no remote-server scan).
4. Focused packet-decoding + switch-off checks; test locally; record what appeared.

Constraints: original server is gone — do not wait for an original cmd132 capture; revived evidence must be `REVIVED_REFERENCE`. Update research docs + this log; run `99_agent_handoff.bat` then `99_gpt20_publish.bat`; commits start with `gpt20:`.

## 2. Files changed (this session)

| File | Change |
|---|---|
| `server/truthan_local_server_v026.py` | Added `TRUTHAN_DATA_SPAWN` (default OFF), `DATA_SPAWN_FIXTURES` (TEST OBJ1014 first), `data_spawn_entities()`; `starter_probe_entities()` returns fixtures only when switch ON; startup banner line. Login/map/movement handlers untouched. |
| `tests/test_v026_baseline.py` | AST loader extended (`pstr`, `npc_view_body`, `data_spawn_entities`); class helpers wrapped in `staticmethod`; fixed pre-existing baseline index/format bugs; added cmd132 field decode + zero-maxMP reject + switch-off/on/wrong-scene tests. |
| `03_test_data_spawn.bat` | **New** — sets `TRUTHAN_DATA_SPAWN=1` + scene 9068, other probes off, calls `01_test_capture.bat`. |
| `docs/PROTOCOL.md` | cmd9 C→S row + “cmd9 vs sprite visibility” section. |
| `docs/RE_FINDINGS.md` | cmd9 caller conclusion + data-driven spawn switch section. |
| `docs/ENTITIES.md` | cmd9 role + `TRUTHAN_DATA_SPAWN` note. |
| `docs/research/MAP_SPAWN_OBJECT_CHAIN_PROPOSAL.md` | Caller note; cmd9 gap reworded as guide/UI only. |
| `PROJECT_STATE.md` | 2026-09-25 research+server blurb. |
| `server/README_v0.26.txt` | DATA-DRIVEN SPAWN section. |
| `loggpt/_current.md` | This file. |

**Unexpected / not authored by this task (reported, not reverted):**

- `opencode.json` — already dirty at session start: adds `external_directory` allow for `TTpc/**` + `BLRM-local-inputs/**` and `edit` deny for those paths; duplicate `"edit"` key; no trailing newline. Valid JSON. Pre-existing working-tree change; publisher will stage it unless the operator reverts it first.

**Not modified:** TTpc, both APKs, Long Tinh reference, login/role/scene/movement packet builders.

Ephemeral (gitignored `/runtime/agent/`): passive screenshots `datas_spawn_passive*.png`; server packet logs under `server/truthan_packet_logs/`.

## 3. Source evidence

### cmd9 callers / sprite visibility — status `PARTIAL` (static call-graph)

| Claim | Evidence | Status |
|---|---|---|
| Only C→S cmd9 encoder is `sendGetNpcListInSceneMessage()` body=`putShort(scene.intId)` | `GameWorld.java:18694` | VERIFIED definition |
| **0 call sites** of that method outside its definition | Grep JADX sources + apktool smali; `SendData(9,` count = 0 | VERIFIED static (inspected tree) |
| cmd9 S→C fills `npcListInScene` only (no `scene.addSprite`) | `processNpcLeadListInSceneMessage` `:8800`; consumers `refreshNpcList:17071`, `readyNpcList:40126` → `FoundNPC.bin`, “NPC引导” `draw2DialogNpcListInMap:4282`, path/transport via `NpcListId` | VERIFIED static |
| Sprite visibility is cmd132 → `processViewSpriteMessage:12681` → `GScene.addSprite:625` + `visibleReady` | DEX/JADX | VERIFIED static |
| Missing map sprites are **not** caused by omitting cmd9 | follows from above | PARTIAL (no runtime A/B) |

Conclusion written to `docs/PROTOCOL.md`: do **not** treat cmd9 as a prerequisite for rendering; it only feeds the NPC guide/list UI.

### Data-driven spawn — status `PARTIAL` (unit-tested; no real-client map entry)

- Switch default: `TRUTHAN_DATA_SPAWN` unset/`0` → `data_spawn_entities()` returns `[]`.
- When `1` and scene 9068: APK-validates fixtures via `_validated_probe_entity` / `verify_probe_object`; first fixture TEST OBJ1014 sprite 220010 at (203,253); `"label"` stripped before send.
- Wrong scene → `ValueError`.
- Startup prints `data-driven spawn fixtures: ON|OFF`.

### Passive client session (step 3) — **no map-entry/entity sequence**

Command: `py -3 tools\truthan_gui.py launch` with `TRUTHAN_DATA_SPAWN=1 TRUTHAN_SCENE=9068` (other probes 0).

Result (real output):

- Fresh server PID 15668; ports 1888/8089/19000/2888/29000 listening; adb reverse recreated.
- Client started `com.t4game/j2ab.android.app.J2ABMIDletActivity`.
- Packet logs 00:28:12: all five ports CONNECTED then immediately `CLOSED ... reason=peer_eof` (no cmd bodies logged).
- Second auth CONNECT 00:28:55 still open at check; `runtime_state.json` remained `game_connected=false`, `npcs=[]`.
- No `cmd=132`, no `PROBE SPAWN`, no `TEST OBJ` in this session’s logs.
- Screenshots (gitignored): `datas_spawn_passive_*.png` (337938 then ~6.2MB frames) — **not inspected as proof of IN_GAME**.
- **No taps / no automation / no remote-server scan.**

Evidence level: **no client test of DATA_SPAWN achieved**; no `REVIVED_REFERENCE` packet sequence recorded. Status remains PARTIAL / UNKNOWN for original spawn identity.

## 4. Commands run (actual results)

| Command | Result |
|---|---|
| `py -3 -m unittest tests.test_v026_baseline` | **Ran 21 tests … OK** |
| `py -3 -m py_compile server\truthan_local_server_v026.py` | COMPILE_OK |
| `py -3 tools\truthan_gui.py launch` (with DATA_SPAWN=1) | JSON success: restarted server, reverse rules, component started |
| Passive wait + screencap + packet-log grep | No cmd132/enter-scene; `game_connected=false` |
| `git status --short` | 9 modified + `?? 03_test_data_spawn.bat` + dirty `opencode.json` |

**Not run:** full interactive `01_test_capture.bat` / `03_test_data_spawn.bat` pause loop (would block non-interactively). No `01_test_capture` debug ZIP this session.

## 5. Blockers and next task

**Blockers:**

1. Passive launch did not reach login/map entry (`peer_eof` on all ports immediately; auth stayed open without cmd277 flow). Original tutorial objectDataId/XY remain `UNKNOWN`.
2. `opencode.json` unexpected pre-existing diff must be accepted or reverted by the operator before/after publish (reported above).
3. BOOTSTRAP real-client `IN_GAME` proof not recorded for this session.

**Exact next task for Codex:**

Run a controlled interactive (or OCR-bootstrap) login after `py tools\truthan_gui.py launch` with `TRUTHAN_DATA_SPAWN=1`, prove `IN_GAME`, then capture one map-entry sequence (cmd32/cmd10/cmd132) using `03_test_data_spawn.bat` / `01_test_capture.bat`. Label any revived-server observations `REVIVED_REFERENCE`. Do not invent tutorial NPC identity. Optionally implement cmd9 mode0 encoder **only** for the guide/list UI if a runtime need appears — not as a sprite prerequisite.
