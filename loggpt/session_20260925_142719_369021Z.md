# GPT-20B handoff — 2026-09-25T14:27:19.369021+00:00

Branch: main
Base commit: 45163ed5115fb04a697111b900070438dce2cde7

# Handoff — scene 9068 playable population

Date: 2026-09-25
Base commit: `45163ed5115fb04a697111b900070438dce2cde7`

## Completed

- Added `docs/research/SCENE_9068_ENTITY_PROVENANCE.md` with row-level TTpc
  source references, candidate Original APK objectDataIds and asset evidence,
  dialogue text/unknown IDs, positions, and explicit VERIFIED /
  REVIVED_REFERENCE / RECONSTRUCTED / UNKNOWN provenance. Targeted TTpc loaders
  and data do not expose a map-to-entity or spawn-coordinate table.
- Added opt-in, data-driven scene population in
  `server/scene_population_9068.json`: three NPCs and two monsters. All
  identity-to-object joins, coordinates, and stats are labeled RECONSTRUCTED.
  Coordinates are validated against decoded 9068 block-0 walkability; object
  image chains are checked against the Original Android 1.17 APK.
- Server sends cmd132 entities after the cmd10 scene-ready response. NPC
  interactions return configured cmd120/cmd73 dialogue. Quest groups are empty.
  cmd137 combat remains unimplemented because nested skill-effect/property
  records are unresolved; no packet layout was guessed.
- Map socket idle timeouts now continue after 60-second polls. Added explicit
  ADB-only bootstrap mode with `--reuse-session --no-create`; the screen watcher
  was stopped.
- Focused tests: 48 passed across `tests.test_map_entry_integration`,
  `tests.test_v026_baseline`, and `tests.test_truthan_ui_control`.

## Client check / blocker

The guarded ADB-only bootstrap made one login exchange and reached ROLE_LIST.
Server cmd20 reported `role_count=0`; no role-create request was sent. The
controller stopped, per the no-create constraint. There was no existing role
available to reuse, so populated scene entry, movement, entity visibility,
selection, and dialogue have not been verified on the target client. No entity
screenshots are published. Do not retry login, create a role, or restart the
server unless the user explicitly changes that constraint.

Trace: `runtime/agent/ui_control/20260925T142301_003429Z/trace.jsonl` (local;
not published). The latest source/test and client status is in `PROJECT_STATE.md`
and `docs/research/SCENE_9068_ENTITY_PROVENANCE.md`.

## Next

1. Run `99_agent_handoff.bat` and retain its complete output.
2. Run the publisher and report `PUSH_OK` only if it prints that exact token.
3. A real-client scene/dialogue check requires an available existing role and a
   valid runtime session under the user's reuse constraints.
