# GPT-20B handoff — 2026-09-24T13:45:16.740874+00:00

Branch: main
Base commit: 9862c52f22715b70e87ce972e2e37a96c8e0b295

# GPT-20B session log

## 1. Task and times

- Task: execute BLRM research task from base `9862c52` — trace PC skill ID
  families 171xx–175xx through TTpc JAR for class/sect + weapon links; inspect
  original (and separately revived) Android 1.17 APK for where damage/MP/
  cooldown/level-req/weapon-restriction values actually come from; produce a
  VERIFIED class→skill→rank→permitted-weapon table plus optional
  DESCRIPTION_ONLY CSV; write results under `BLRM/docs/research/`; update core
  docs; write this file; run `99_agent_handoff.bat` then `99_gpt20_publish.bat`.
- Start: 2026-09-24 after previous publish `9862c52f22715b70e87ce972e2e37a96c8e0b295`
  (session log `session_20260924_132650_083162Z.md`).
- End: this log, immediately before `99_agent_handoff.bat` / `99_gpt20_publish.bat`.
  Wall clock ~13:44 UTC.

## 2. Base commit and files changed

- Base: `9862c52f22715b70e87ce972e2e37a96c8e0b295` (`gpt20: publish session 2026-09-24 13:26 UTC`), branch `main`, tree clean at start.
- Files changed (not yet committed until publisher):
  - **New** `docs/research/SKILL_CLASS_WEAPON_MAP.md` — three-location provenance catalog:
    occupations/weapon enums; family→class = UNKNOWN (no static map); packet-driven
    weapon/job/MP/CD/level fields; honest class table with UNKNOWN cells; non-claims.
  - **New** `docs/research/skill_family_catalog.csv` — DESCRIPTION_ONLY, 384 data rows
    UTF-8: id/family/name_vn/name_e/desc tags/needWeapon empty/rank group bounds.
  - **Edited** `docs/research/PC_SKILLS_EQUIPMENT_QUESTS.md` — follow-up section 6 → renumbered artifacts to 7.
  - **Edited** `docs/RE_FINDINGS.md` — skill families/classes/weapon limits checkpoint.
  - **Edited** `PROJECT_STATE.md` — follow-up research blurb; no server change.
  - **Edited** `docs/PROTOCOL.md` — skill/job/weapon client command constants table
    (27/128/129/287/312/316/340) after cmd132 callee audit (table order fixed so the
    original callee rows stay contiguous).
  - **Edited** `docs/ENTITIES.md` — character classes / skills section.
  - **Edited** `docs/QUESTS.md` — pointer to skill/class/weapon catalog.
  - **New** this file `loggpt/_current.md` (publisher rotates to session log).
- Temp working files (outside repo, not published raw decrypts):
  `C:\Users\Mech\AppData\Local\Temp\opencode\{SkillCsv,SkillCatalog2,FamilyNames}.java`,
  `skill_family_catalog.csv`, `_out.txt`, `gameplay_javap\skilltree_main_b_a_g_{g,h}.txt`,
  `jar_scan2\`, `revived_dex\classes.dex`.
- **Not modified:** `server\truthan_local_server_v026.py`, TTpc tree, BLRM-local-inputs,
  original APK, any protocol/spawn IDs. No raw decrypted table contents published.

## 3. Evidence (paths / methods / status)

| Finding | Evidence | Status |
|---|---|---|
| Occupations = 力士/刺客/唤雷师/天师/武神/圣仙 | original jadx `CommonConstants.OCCUPATION_NAMES:598`; constants 0–3 at 329–332 | VERIFIED |
| Create-role uses first 4 only | `RoleLogin.java:307/:948` — `OCCUPATION_NAMES.length - 2` | VERIFIED |
| Jobs 4–5 = NewJob/化神 | `NewJob.java:31`; `GameWorld.java:4648` `user.job > 3`; `GUser.java:206–208` | VERIFIED (existence; unlock rules incomplete) |
| `user.job` from packet byte | `GameWorld.java:6113` `setJob(readBuffer.getByte())` | VERIFIED |
| WEAPONTYPE_NAMES = 刀/剑/斧/法剑/羽扇/法杖 | `CommonConstants.java:606` | VERIFIED |
| Equipped weapon type on outfit slot 4 | `RoleOutfit.weaponTyte:9`; filled `GameWorld.java:6147` / `NewJob.java:116` | VERIFIED |
| **No static family→class map** in PC conf or original DEX | string/constant scans for 171xx–175xx keyed by job; no arrays | UNKNOWN ownership (explicit) |
| Create-role display / physical-magic UI split | `GameWorld.java:14577` job<2 or ==4 → physical crit labels | VERIFIED (display only) |
| Skill weapon gate at cast | `testWuQiXianZhi` `GameWorld.java:21214` | VERIFIED (runtime check) |
| Skill job gate at cast | `testJobXianZhi` `:21220` | VERIFIED |
| Weapon/job lists filled from packets | `readRoleQuickAction` `:16645` → `setWuQiXianZhi` `:16713–16719`, `setJobXianZhi` `:16721–16727` | VERIFIED |
| Skill detail weapon byte list | `processSkillDetail` `:11069–11077`; `processCommSkillDetailMessage` `:4328–4336` (cmd 312) | VERIFIED |
| MP/CD quick slot | `QUICK_SKILL_consumeMP/HP` `:16682–16683`; CD `:16690` | VERIFIED (packet fields) |
| Level req / learned skill fields | `readRoleSkill` `:16750` — id short, name, maxLevel, limitLevel, actionType, level, iconId | VERIFIED |
| Genius tree from packets | `readGeniusTree` `:16612`; cmds 27/128/287 | VERIFIED |
| SKILL_RESTRICT=316 is sprite state, not skill table | `processLimitSkillMessage` `:6222` | VERIFIED (rename trap) |
| GSkillData rank/rankData unused | no external smali refs to `Lcom/t4game/GSkillData` | PARTIAL (no reflective use proven) |
| PC SkillTree network rank/level | `main.b.a.g.g` logs; reader `a/b` on `main.b.a.f.M`: id,lv,rank,treeCol,treeRow,needSkillId | PARTIAL (full opcodes not mapped) |
| Family row counts 171=80,172=22,173=97,174=100,175=85 | AES-decoded `zsSkills_language.dat` via SkillCsv (read-only) | VERIFIED |
| Adjacent same-name IDs = rank-step groups (size 6 or 2); 17196–17201 crosses family boundary | same | VERIFIED structure (DESCRIPTION_ONLY names) |
| needWeaponType empty all 384 | prior SkillStats + CSV | VERIFIED |
| Equipment jobCanUse / limitUseLevel / weaponTyte from packets | `RoleGoods.setCanUse`; `RoleOutfit` outfit apply | VERIFIED |
| Damage % | only description prose (e.g. 17101–17106 120%…160%) | VERIFIED as text; formula UNKNOWN |
| Revived APK same occupation vocabulary | UTF-8 string scan of `TruThan.apk` classes.dex hits 力士…圣仙 | PARTIAL |
| Class→family assignment | **not inferred** from ID prefix or name | UNKNOWN (by rule) |
| Server-side source of skill numerics | not in this repo scope this session | UNKNOWN |

## 4. Commands run and real results

- Mandatory reads: AGENTS.md, AI_RULES.md, LOCAL_AGENT_RULES.md, PROJECT_STATE.md,
  docs/GPT20_HANDOFF.md, docs/research/PC_SKILLS_EQUIPMENT_QUESTS.md — done.
- `git status` clean at start; `git log --oneline` top = `9862c52`.
- jadx sources under `build\research\jadx\sources\com\t4game\` read for
  CommonConstants, GameWorld, RoleQuickUse, RoleGoods, RoleOutfit, RoleSkill,
  GSkillData, GGenius*, MessageCommands, RoleLogin, NewJob, GUser.
- smali scan: no external references to GSkillData (outside its own file).
- PC jar: `javap -cp ZhuShen-1.0.0.jar` on `main.b.a.g.g` / `main.b.a.g.h` / `main.b.a.f.M`.
- AES-ECB key `1234567812345678` decode of `zsSkills_language.dat` in temp Java
  (`SkillCsv.java` etc.); CSV written UTF-8 (strict decode OK; bytes `C490`=Đ).
- CSV copied to `docs/research/skill_family_catalog.csv` — 385 nonempty lines
  (header + 384).
- Fixed PROTOCOL.md mid-table insert so callee audit table rows stay contiguous.
- **Not run:** emulator/runtime game test, `01_test_capture.bat`, server tests
  (static research only; BOOTSTRAP phase gate N/A for source-only task).
- Publisher not yet run when this paragraph was written; see bottom of publish
  BAT output for PUSH_OK.

## 5. Blockers and exact next task

**Blockers:**

1. Family→class ownership still UNKNOWN — needs captured packets from a real
   logged-in client per job (quick-slot jobXianZhiList / skill detail weapon lists).
2. Server-side damage/MP/CD formulas still UNKNOWN (not in client conf).
3. PC `Tasks_language.dat` still absent everywhere (carried).
4. Revived APK only PARTIAL vs original at method level.

**Next recommended action:**

- Capture real-client skill detail / quick-slot packets for each create-role
  occupation (BOOTSTRAP → login → open skill UI → log cmd27/129/312 payloads)
  to fill permitted weapon/job cells; do not invent class→family from names.

**Current blocker for automation phases:** BOOTSTRAP real-client PASS still not
recorded (unchanged; this session was static research only).
