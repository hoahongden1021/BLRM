# GPT-20B handoff — 2026-09-25T16:44:28.898360+00:00

Branch: main
Base commit: e743de9de3488cc04dda675d528a4e1bd58fbd4b

# GPT-20B handoff - 2026-09-26T01:43:12+09:00

Branch: main
Base commit: e743de9de3488cc04dda675d528a4e1bd58fbd4b

# Handoff - real-client NPC interaction (cmd120/cmd73) verified

Date: 2026-09-26 (01:00-01:43 +09:00)
Task: run the scene 9068 NPC interaction test on the real Android client
against the still-running local server, verify the five entities visually and
via packets, document/validate the cmd137 layout before combat work, then
record the real session results and publish.

## Completed

- Kept the single LocalServer v0.26 process (PID 12280, ports 19000/29000)
  alive for the whole test. Restarted only the client with
  `py tools\truthan_ui_control.py bootstrap --reuse-session --adb-only
  --restart-client` -> `BOOTSTRAP_PASS`, `role_count=1`, `create_sent=false`,
  `in_game=true`, WORLD_HUD, five `POPULATION_RECORD` lines, live log
  `server/truthan_packet_logs/20260926_011006_p19000_127.0.0.1_52422.log`.
- Root-caused the previous failure: `server/scene_population_9068.json` had
  `"flags": []`, so `GameWorld.ReadNpcFlagFuction()` (`:14100`) returned null
  and `onPointerCheckNPC()` (`GameWorld.java:2685`) returned before
  `sendGetNpcMissionListMessage()` (`:2691`). The three NPC records now carry
  `"flags": [5]`; the two monster records keep `[]`.
- Confirmed the change on the wire: the three NPC cmd132 frames grew by
  exactly one byte (77 -> 78, 74 -> 75, 75 -> 76), the two monster frames did
  not (71, 90), and the NPC flag block decodes to count `01`, value `05`.
- Derived and measured the display geometry from live screenshots: app content
  box device x `393..2758`, y `0..1271` (black band starts at row 1276),
  `CANVAS_W = 789`, `CANVAS_H = 424`, scale 3.0, `win_x = 0`,
  `win_y = mapY - 212`. Tap -> walk transform confirmed twice against cmd133
  targets: `(930,750)` -> `(176,240)` and `(990,630)` -> `(192,226)`, both
  equal `(tile*16, tile*16 + win_y)`.
- **NPC interaction VERIFIED end to end.** Double tap at device `(990,630)`
  from player `(192,226)` (hit box of NPC 230011: device x `900..1044`,
  y `480..636`) -> RX `cmd=120`, 16-byte frame, body = i32 `0003827b` =
  230011 -> server reply 103 bytes -> client rendered "Guide Fairy" +
  greeting + `1.Talk`. Tapping `1.Talk` at `(1112,720)` -> RX `cmd=73`,
  same 4-byte body -> server reply 86 bytes -> client rendered title
  `Starter area` and text `This local guide has no recovered original quest
  records.`, byte-identical to `interaction.title` / `interaction.text` in the
  JSON.
- Scene 9068 population verified from the same log: 230010 Beginner Guide
  @170,232 obj1014; 230011 Guide Fairy @190,216 obj1015; 230012 Village Head
  @190,248 obj1016; 230110 Fire Fox @210,232 obj1155; 230111 Reconstructed
  Creature 2077 @210,248 obj2077.
- Documented and cross-checked the combat packet layouts before any combat
  implementation: cmd136 request body is **19 bytes** (`docs/PROTOCOL.md`
  previously said 18 - corrected), and the complete cmd137 reader was written
  out from `processSpriteSkillResultMessage` (`:11411`,
  `case 137` at `:7311`), `readSkillActionSEData` (`:13177`) and
  `readSpriteUpdateProperty` (`:13187`).
- Published redacted evidence screenshots:
  `docs/research/ui_control_images/run_20260926_npc_cluster.png`,
  `run_20260926_npc_anchors.png`, `run_20260926_npc_function_list.png`,
  `run_20260926_npc_talk.png`.

## Files changed

| File | Change |
|---|---|
| `server/scene_population_9068.json` | three NPC records `flags: []` -> `[5]`; monsters unchanged |
| `tests/test_v026_baseline.py` | population test now asserts NPC `flags == b"\x05"` and monster `flags == b""` |
| `tools/truthan_ui_control.py` | pre-existing uncommitted work: `name_visible()` cursor-tolerant OCR check, session-scoped creation guard (`role_action(..., session)`, `creation_record_blocks(record, session)`, `creation_record(session)`) |
| `tests/test_truthan_ui_control.py` | pre-existing uncommitted work: tests for `name_visible` and the session-scoped guard |
| `docs/research/NPC_INTERACTION_HANDOFF.md` | rewritten from "no live test" to the live result, request/reply layouts, flag root cause, selection geometry, status split |
| `docs/PROTOCOL.md` | command table rows for 120/73/136/137; new `2026-09-26: live cmd120 / cmd73 verification`; corrected cmd136 to 19 bytes; full cmd137 field table |
| `docs/ENTITIES.md` | new `2026-09-26: NPC flag value correction and live dialogue proof` |
| `docs/RE_FINDINGS.md` | new `2026-09-26: client screen geometry, tap transform, NPC interaction` |
| `PROJECT_STATE.md` | new latest milestone (2026-09-26); corrected the superseded `role_count=0` / `peer_eof` blocker text |
| `docs/research/ui_control_images/run_20260926_*.png` | 4 new redacted screenshots (player name blacked out) |

No server source change was needed: the existing `npc_function_list_body()`
and `npc_talk_body()` already matched the client readers.

## Source evidence (paths / methods / status)

- `VERIFIED` - `GameWorld.java:18714` `sendGetNpcMissionListMessage` and
  `:19519` `sendNpcFunctionTalkMessage` write one i32; confirmed by two live
  16-byte request frames with body `0003827b`.
- `VERIFIED` - `processNpcFunctionListMessage` (`:8514`, `case 120` at
  `:7269`) and `processNpcFunctionTalkMessage` (`:8676`, `case 73` at
  `:7149`): the live 103-byte and 86-byte replies decode field for field and
  the client rendered the decoded strings.
- `VERIFIED` - `GameWorld.java:2627` `onPointerCheckNPC`, gates at `:2685`
  (`gnpc.Flag == null`, `testInterRound(..., 80)`) and `:2688`
  (flag 4 with length 1); `:2691` is the cmd120 send.
- `VERIFIED` - `CommonConstants.java:221` `NPC_FLAG_TALK = 5`, `:314`
  `NPC_FUNCTION_TALK = 7`, `:616` `NPC_FLAG_FUNCTION[5] = {7}`.
- `VERIFIED` - `GameWorld.java:15412` `onPointerSelectNPC` hit box
  `[u-21, u+27] x [v-42, v+10]`, `GScene.addToView:651` mapY-ascending order.
- `VERIFIED` - screen geometry: `GScene:67 setWinDimension`,
  `GScene.setWinPos:1987` clamping, `GUser:680`, `GMap:50-53`,
  `Defaults.setResolutionRatio` callers (`BeforeGame:21`, `FlashScreen:40`,
  `MainMenu:84`), measured from the live screenshot content box.
- `PARTIAL` - `MessageCommands.java:589-591`
  (`SPRITE_SKILLRESULT_MESSAGE = 137`, `SPRITE_SKILL_MESSAGE = 136`,
  `SPRITE_SKILL_LEAD_MESSAGE = 143`); `case 136` / `case 137` dispatch at
  `GameWorld:7308` / `:7311`.
- `PARTIAL` - cmd136 request layout (`sendAttackMessage`, `GameWorld:13429`,
  19 bytes) and the full cmd137 reader: static client source only, no frame
  exists in any capture under `server/truthan_packet_logs/`.
- `UNKNOWN` - original NPC/monster identity, original quest data, original
  names. Everything configured is `RECONSTRUCTED`. Server combat response and
  quest progression remain `UNKNOWN` and no packet was invented.

## Commands run and real results

- `py tools\truthan_ui_control.py bootstrap --reuse-session --adb-only
  --restart-client` -> `BOOTSTRAP_PASS`, `role_count=1`, `create_sent=false`,
  `in_game=true`. No role was created.
- `adb shell input tap 990 630` twice (450 ms apart) -> 6 new RX frames,
  including `cmd=120`; `last_npc_request = {"cmd":120, "npc_id":230011,
  "interaction_available":true}`; player stayed at (192,226).
- `adb shell input tap 1112 720` -> 1 new RX frame `cmd=73`;
  `last_npc_request = {"cmd":73, "npc_id":230011, ...}`.
- `adb shell screencap` + `py tools\truthan_rapidocr.py` on the two dialog
  screenshots -> OCR text matches the configured greeting / `1.Talk` /
  `Starter area` / guide text.
- `py -m unittest tests.test_truthan_ui_control tests.test_v026_baseline
  tests.test_map_entry_integration` -> **Ran 50 tests, OK** (first run had 1
  failure because `test_data_spawn_on_loads_full_reconstructed_population`
  still asserted `flags == b""`; the assertion was corrected and the suite
  re-run green). `INTEGRATION_OFF_OK no_cmd132`,
  `INTEGRATION_ON_OK {230010: (170,232,1014), 230011: (190,216,1015),
  230012: (190,248,1016), 230110: (210,232,1155), 230111: (210,248,2077)}`.
- Not run: `01_test_capture.bat` (the live interactive run above is the
  evidence for this task), `99_agent_handoff.bat` and `99_gpt20_publish.bat`
  are run after this log is written.

## Unresolved blockers / limits

- cmd136 and cmd137 have never been captured, so combat remains untested and
  the documented layouts are static contracts only.
- Sprite-pixel identity is `PARTIAL`: four distinct bodies are separable in
  the cluster image while six anchors are predicted, so 230012 and 230111
  cannot be attributed to individual pixels from that screenshot alone.
- The `canCaiJi` collection branch and every non-talk NPC function
  (trade/shop/relive/quest) are untested; quest groups in the cmd120 reply
  are four zero bytes by design.
- The talk dialog was left open at the end of the run (right/left soft-key
  taps at device y=1180 did not visibly dismiss it); the next session may
  need a clean dialog dismissal before further world interaction.

## Exact next task for Codex

Implement and capture the first real combat exchange: send a `cmd136`
19-byte attack request from the client (normal attack against monster 230110
"Fire Fox" at 210,232), then implement the server `cmd137` reply strictly from
the field table now in `docs/PROTOCOL.md` (flags, src/tgt id+type, unused i16,
moveMode, optional dest pair, src hp/mp, unused i8, dir, 3 skill-effect
records, property count, property records with the 3 extra bytes for
spriteType 3). Do not invent values that are not either observed from the
client's own request or required to be self-consistent (hp/mp/maxHp/maxMp from
the server's own state). Capture the frame, verify the client renders the hit
animation and damage number, then update `docs/PROTOCOL.md` from `PARTIAL` to
`VERIFIED` for cmd137 and continue the same `loggpt/_current.md` ->
`99_agent_handoff.bat` -> `99_gpt20_publish.bat` workflow.
