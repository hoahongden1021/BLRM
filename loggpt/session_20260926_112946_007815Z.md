# GPT-20B handoff — 2026-09-26T11:29:46.007815+00:00

Branch: main
Base commit: 538ac7f2c3e353c005b8a6b6b5cd72e4cdfbe564

# GPT-20B session log — 2026-09-26: live combat verification (Fire Fox, scene 9068)

## 1. Task and times

Task: make one existing reconstructed monster (230110 "Fire Fox", scene 9068)
attackable through the real Android 1.17 client — select -> normal attack ->
visible HP reduction -> death -> respawn — server-side behind an off-by-default
switch, with real-client evidence and focused unit tests; then update docs,
run the mandatory handoff, and publish.

Times (UTC, artifact-anchored): earliest work before bootstrap trace
`20260926T101534_864458Z` (10:15:34), final verification publish window
2026-09-26 11:25 UTC. Base commit at session start:
`538ac7f gpt20: publish session 2026-09-26 04:56 UTC`.

## 2. Files changed and what each change does

Modified:

- `PROJECT_STATE.md` — new latest milestone: combat VERIFIED end to end
  (select -> cmd136/137 -> hp 60/45/30/15/0 -> death -> +5 s cmd132 respawn,
  two full cycles, NPC protection); NPC-interaction milestone demoted to
  "Prior milestone"; removed stale "combat response stays UNKNOWN" text.
- `docs/PROTOCOL.md` — cmd136 section status PARTIAL -> **LIVE-VERIFIED**
  (19B request / 66B response confirmed against real frames, hp trace,
  hit_result=5); cmd137 reader status PARTIAL -> VERIFIED with live
  validation facts; removed "no frame exists in any capture".
- `docs/ENTITIES.md` — scene 9068 population section: combat implemented and
  VERIFIED live with the exact tap point, tunables, protection behaviour and
  screenshot paths; "no real-client visual check has passed" removed.
- `docs/QUESTS.md` — dialogue capture is now VERIFIED (2026-09-26 cmd120/73
  byte match); combat no longer a blocker; quest IDs/wire layouts remain the
  evidence gap.
- `docs/RE_FINDINGS.md` — stale cmd136/137 paragraph corrected; appended
  dated section "live combat verification + controller fixes" with the full
  sequence, geometry, tunables, evidence paths and the three fixes below.
- `server/truthan_local_server_v026.py` — (from earlier in this session)
  combat integration: cmd27 push when `TRUTHAN_COMBAT=1`, cmd136 -> cmd137,
  `poll_respawns()`, cmd381 logging, `_runtime_npc_rows` hp/kind fields;
  **fix**: `respawn_in_s` mixed clocks (`respawn_at` is monotonic ms but the
  writer subtracted `time.monotonic()` seconds -> garbage ~2.7e8); now
  `(respawn_at - monotonic*1000)/1000`.
- `tools/truthan_ui_control.py` — (earlier in session) env-gated local-test
  credential fill (`credentials()`, `LOGIN_INPUT_BANDS/TAPS`,
  `fill_credentials()`, `field_hint_matches()`); EN CREATE_ROLE
  classification tolerance for the OCR `lame`/`Name` misread; **fix this
  run**: `bootstrap --no-create` now passes `None` as the creation ledger so
  the user's `--no-create` refusal wins over a stale live-run ledger.
- `tests/test_truthan_ui_control.py` — credential-fill tests,
  `test_classify_create_role_en_tolerates_lame_ocr` (32 tests in file).

New:

- `server/truthan_combat.py` — combat session/engine: parse cmd136 (19B),
  validation (monster-only, can_hit, range, cooldown), RECONSTRUCTED env
  tunables (`TRUTHAN_COMBAT`, `RANGE=64`, `COOLDOWN_MS=500`, `DAMAGE=15`,
  `RESPAWN_MS=5000`), hp mutation in place, `due_respawns()`.
- `tests/test_truthan_combat.py` — 18 unit tests (encoders/decoders,
  validation rejects, death/respawn timers).
- `tests/test_combat_integration.py` — real-socket integration: cmd27 push,
  hp trace `[45,30,15,0]`, death -> cmd132 respawn at +1.5 s (test config),
  second life, clean-mode ignores cmd136, and a regression guard that
  `respawn_in_s` stays within 0..1.5 s (would fail on the ms/s bug).
- `docs/research/ui_control_images/run_20260926_en_combat/` —
  `combat_hp45.png`, `combat_hp30.png`, `combat_hp15.png` (target-frame HP
  bar draining, "Fire Fox" nameplate).
- `docs/research/ui_control_images/run_20260926_en_combat_final/` —
  `combat_final.png` (floating `-15` damage numbers x2 during the final
  verified run).

No unexplained new files. Temp scripts stayed in `%TEMP%\opencode\`
(`ocr_any.py`, `combat_try.py`, `hp_sample.py`, `shot_loop.py`) — outside the
repository, nothing to clean. Packet logs and `runtime/agent/` are
gitignored as before.

## 3. Evidence (path / method / status)

- **VERIFIED — end-to-end combat, real client.** Fresh runtime
  (`py tools\truthan_gui.py launch`, package `com.t4game.en`) + `bootstrap
  --adb-only` -> `BOOTSTRAP_PASS`, `WORLD_HUD` (trace
  `runtime\agent\ui_control\20260926T111205_768152Z\trace.jsonl`; earlier
  pass `20260926T101534_864458Z`). Fast double tap at device `(860,564)`
  selected+confirmed 230110; RX `cmd=136` (19B) every ~1 s, TX `cmd=137`
  (66B) each time, `hit_result=5`, `damage=15`; hp `60->45->30->15->0`;
  respawn TX `cmd=132` at exactly +5 s with full HP; second kill cycle on
  230111/230110; `respawn_in_s` telemetry `5.0->3.99->2.97->1.96->0.94->60`.
  Packet logs: `server/truthan_packet_logs/20260926_191043_p19000_127.0.0.1_56201.log`
  and `20260926_201236_p19000_127.0.0.1_54782.log` (184/184 cmd136/cmd137
  pairs in the latter). Method: adb input taps, `runtime_state.json`
  sampling, screencap; client jadx (`GameWorld.java:2627/:15412/:21133/
  :13429/:13840`) for semantics.
- **VERIFIED — on-screen damage.** `-15` damage numbers x2 and the
  target-frame HP bar captured:
  `docs/research/ui_control_images/run_20260926_en_combat*/`.
- **VERIFIED — monster-only protection.** Server rejects cmd136 against
  230010-230012 (`npc_protected`); the auto-hunt never targeted them in
  either packet log; unit + integration tests assert it.
- **VERIFIED — `respawn_in_s` fix.** Live countdown within the 5 s window
  after the ms/s fix; regression-guarded in `test_combat_integration.py`.
- **VERIFIED — `--no-create` precedence and CREATE_ROLE `lame` OCR fix.**
  32 controller tests green after both changes.
- **VERIFIED — tests.** `py -m unittest tests.test_truthan_ui_control
  tests.test_v026_baseline tests.test_map_entry_integration
  tests.test_truthan_combat tests.test_combat_integration` ->
  `Ran 81 tests ... OK` (run twice, after all fixes).
- **PARTIAL — absolute map->screen constants.** The documented
  `CANVAS_W=789/CANVAS_H=424` derivation was not re-verified on this
  2992x1344 device; the working tap point `(860,564)` was derived
  empirically from the live Fire Fox nameplate relative pick box
  (48x52 canvas units, release-coords). It is evidence-based for the current
  camera/layout but not a proven universal formula.
- **UNKNOWN (unchanged) — quest IDs / quest wire layouts.** No recovered
  quest records exist; nothing quest-side was fabricated or implemented.
- **Historical note:** the red-vs-yellow damage-number colour was not
  asserted; the captured floating numbers read `-15`, matching
  `TRUTHAN_COMBAT_DAMAGE=15`.

## 4. Commands run and real results

- `py -m unittest tests.test_truthan_ui_control tests.test_v026_baseline
  tests.test_map_entry_integration tests.test_truthan_combat
  tests.test_combat_integration` -> **`Ran 81 tests in 12.1s / OK`** (final
  run after every code+doc change). Earlier in the session: 3 failures
  (stale-ledger `--no-create`), fixed, re-ran green.
- `py tools\truthan_gui.py launch` (with `TRUTHAN_PACKAGE=com.t4game.en`,
  `TRUTHAN_DATA_SPAWN=1`, `TRUTHAN_SCENE=9068`, `TRUTHAN_COMBAT=1`,
  `TRUTHAN_LOCAL_TEST_CREDENTIALS` env) -> launcher JSON: adb reverse rules
  recreated, client activity started.
- `py tools\truthan_ui_control.py bootstrap --adb-only` (same env) ->
  `BOOTSTRAP_PASS` / `WORLD_HUD`, cmd27 pushed, five cmd132 NPC rows with
  hp/max_hp/alive.
- adb double tap `860 564` (+0.6 s) then `runtime_state.json` sampling ->
  hp trace with respawn countdown as above; `screencap` ->
  `combat_final.png`; ground tap `1400 600` -> hunt stopped (hp stable 60
  across samples, no new cmd136).
- `cmd /c 99_agent_handoff.bat` and `cmd /c 99_gpt20_publish.bat` are run
  immediately after this log, per workflow; publisher output is the
  authoritative result (expect `PUSH_OK`/`BRANCH`/`LATEST_LOG`/`LOG_COUNT=2`).
- Not run this session: APK rebuild/asset work, quest-path tests (no quest
  implementation exists), NAVIGATION/INTERACTION controller subcommands
  (only `status/observe/bootstrap/move` exist).

## 5. Blockers and exact next task

Blockers: none for combat. Open evidence gaps (by policy, not bugs):
quest IDs and quest request/response layouts are UNKNOWN; absolute canvas
geometry constants are PARTIAL (empirical tap point used instead); player
damage reception / player death is not implemented (client HP stayed 100 in
all captures — no source evidence for monster->player attacks yet).

Exact next task: per the automation phase gate, implement **NAVIGATION and
INTERACTION controller automation** — a `move` verification path that proves
cmd133 displacement through `observe`, and an `interact` subcommand encoding
the verified NPC/monster selection flow (observed pick boxes, double-tap
confirm, `--wait` for cmd120/cmd136 side effects) on top of the already
passing BOOTSTRAP — then, only if quest-ID evidence is found in DEX/packets,
start the clearly labeled reconstructed quest.
