# GPT-20B handoff — 2026-09-24T13:04:04.007373+00:00

Branch: main
Base commit: d4aa290047909a9c0d370f61c6ad771b2c655d5f

# GPT-20B session log

- Task: Map/ModelID join research across three locations (TTpc PC jar, BLRM-local-inputs revived TruThan.apk, BLRM original Android 1.17 APK). Deliver evidence table for ModelID → appearance/object → map ID → spawn coordinates; no guessed spawns; no server edits.
- Start/end (UTC): 2026-09-24 approx. 11:40–12:05 UTC (session wall time; publisher stamps final UTC).
- Base commit: `d4aa290047909a9c0d370f61c6ad771b2c655d5f` (`gpt20: publish session 2026-09-24 12:38 UTC`).
- Branch expected: `main`.

## Files changed

| File | Change |
|---|---|
| `docs/research/MODELID_MAP_SPAWN_JOIN.md` | **NEW** — three-location identity (path+SHA-256), asset hash matrix, full evidence table with per-link VERIFIED/PARTIAL/UNKNOWN/REJECTED, explicit non-joins, residual unknowns. |

No server files, BAT files, or docs other than the new research file were modified.

Session-created temporary content kept **outside** the repo (`C:\Users\Mech\AppData\Local\Temp\opencode\`): `DumpMap.java`, harness class files, `jarcls\` full jar extract, `gg_full.txt`, `DumpMap_out.txt`, backup of a stray `conf/` extract that had appeared under BLRM (moved out; `git status` clean of it before publish).

Unexpected pre-existing file handling: none new at publish time. An earlier session moved stray `client_config.properties` out of the repo; still outside.

## Commands run (real results)

- `git status --short` / `git log --oneline -5` — clean base at `d4aa290` before edit; after edit only `?? docs/research/MODELID_MAP_SPAWN_JOIN.md`.
- `Get-FileHash -Algorithm SHA256` on both APKs — values recorded in research doc §1.
- Python `zipfile` scans — obj 377/377/377 all hash-equal; scn/img/conf comparisons as in doc §2; original APK has **no** conf zs tables and DEX has no `ModelID`/`zsNpc`/`map_id_remap`.
- `javap -c/-v` on PC jar loaders (`t`,`i`,`I`,`y`,`v`) and packet classes `main.b.a.g.g`, `main.b.a.g.i`, appearance `main.b.a.e.a.y` / `o` / `d`.
- Runtime harness with jar’s own loaders (`DumpLang`/`DumpMap` + Gdx.files proxy, base `C:\Users\Mech\Desktop\TTpc\assets`): NPC 512 / monster 624 / map 306 / dialogue 447 rows; `REMAP 700->9068 400->7003 900->10046`. First DumpMap attempt with base `TTpc` failed path resolve (`raw=-1`); retry with `TTpc\assets` succeeded.
- Case-sensitive zip read: `i.class` = `zsDialogue_language.dat`, `I.class` = `zsMapWorld_language.dat`, `t.class` = `zsMap_language.dat` (Windows case-fold trap documented).
- `py -3 -m py_compile server\truthan_local_server_v026.py` — exit 0 (static only; **server not launched**, not a client test).
- JADX reads: `GameWorld.java` enter-scene / GNPC / NpcLeadList; `GSprite.setObjectDataId`; `GDataManager` / `GBaseData.initFromJar` path `/data/<type>/<id>.<type>`.
- Sample gzip `.scn` decompress — map grid only; no ModelID/spawn strings.
- **No** `01_test_capture` / emulator run this session.

## Evidence level

- Research doc: **PARTIAL overall chain**; individual links labeled in the doc (map remap + language decode + original object/scene packet paths VERIFIED; ModelID→object join UNKNOWN/REJECTED as single-file chain; spawn XY UNKNOWN from client files).
- Prior NPC/monster language research already published at `d4aa290` — not repeated as new claims.
- Status vocabulary only: VERIFIED / PARTIAL / CANDIDATE / UNKNOWN / REJECTED.

## Blockers

1. Original NPC/mob **spawn coordinates are not in any of the three client trees** — need packet captures or server-side data, not more client asset mining.
2. **No ModelID → objectDataId table** found — if the server must speak ModelID, the mapping must come from non-client evidence.
3. Original 1.17 does **not** use `map_id_remap`; revived APK does (like PC) — server must treat the two Android clients differently for scene ids.
4. This session has **no real-client PASS** for any new claim.

## Exact next task for Codex

Prepare a capture-focused task: login + enter scene on original Android 1.17 and record enter-scene `sceneId` short, cmd132 GNPC `objectDataId`/X/Y, and cmd9 NpcList rows — to bind packet fields against `docs/research/MODELID_MAP_SPAWN_JOIN.md` unknowns. Do not invent spawn tables. End by rewriting `loggpt/_current.md` and running `cmd /c 99_agent_handoff.bat` then `cmd /c 99_gpt20_publish.bat`.
