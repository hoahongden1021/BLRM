# GPT-20B handoff — 2026-09-25T11:02:01.990316+00:00

Branch: main
Base commit: 124f2d1305a812df430ade67445b5e85c18cd407

# Current Handoff

## 2026-09-25 — Watcher/UI observation fix

- Base reviewed: `124f2d1`.
- Watcher OCR observations now carry the unchanged video image for the same
  `frame_id`; controller checks image/OCR identity and dimensions before
  mapping boxes. Stale watcher results and expired label actions use fresh ADB
  screenshot/OCR without weakening freshness guards.
- Regression: delayed watcher observation falls back under `--source auto`.
  Focused compile passed; `py -m unittest tests.test_truthan_ui_control` ran
  14 tests, all passed, including a final pre-input expiration guard test.
- Emulator: fresh launch passed; watcher started as PID 15428. One controller
  action selected `开始游戏` at `[2420, 578]`; watcher evidence was stale, so
  the tap correctly used fresh ADB OCR. Screenshot/OCR timestamps were
  `1790333636.5408962` / `1790333638.3260868`; action return code 0; verified
  `ACCOUNT_LOGIN` at `1790333648.351935` / `1790333650.1790867`.
- A subsequent read-only watcher/ADB comparison agreed on `ACCOUNT_LOGIN`,
  but watcher evidence again fell back to ADB. No tap used a stale frame. The
  10-minute soak was not repeated. See
  `docs/research/WATCHER_UI_OBSERVATION_FIX.md` for exact commands and trace.
- Handoff and publisher scripts still need to run. Do not report `PUSH_OK`
  unless the publisher prints it.
