# GPT-20B handoff — 2026-09-25T12:36:44.581845+00:00

Branch: main
Base commit: 8164e0035247341edead68e9dbee876de45b8a1b

# BLRM handoff — resumed real-client fixture test

Date: 2026-09-25
Start: exact conversation start was not captured; first live status command was 21:24 JST.
End: 2026-09-25 21:34:47 JST
Branch/base at start: `main` / `8164e00` (`gpt20: publish session 2026-09-25 11:05 UTC`)

## Worktree changes

The worktree already contained uncommitted edits in `tools/truthan_ui_control.py` and `tests/test_truthan_ui_control.py`; they were preserved. Those edits include OCR classification for the observed name prompt, existing-role ledger handling, and re-observation/rematching for delayed label taps. This continuation added a single fresh-observation/rematch attempt to `Controller.move()` only when the action freshness guard rejects before ADB input, plus a regression test for that path.

Files changed:

- `tools/truthan_ui_control.py`
- `tests/test_truthan_ui_control.py`
- `loggpt/_current.md`

No server, BAT, protocol, or APK files were changed.

## Real-client evidence

- Kept the existing server process PID `25080` (start time 2026-09-25 20:47:01 JST); did not relaunch the server. The app-only command `py tools\truthan_ui_control.py bootstrap --restart-client` exited 0 and reached `BOOTSTRAP_PASS` / `WORLD_HUD` / `IN_GAME` in scene `9068`.
- The current game connection log is `server/truthan_packet_logs/20260925_212748_p19000_127.0.0.1_64299.log`. At 21:28:05 JST it contains two server `cmd=132` frames, each 76 bytes. The current runtime state records `TEST OBJ1014` (ID 220010, objectDataId 1014, 203,253) and `TEST OBJ1155` (ID 220011, objectDataId 1155, 196,238), both source `server_probe_cmd132`, in scene 9068. The session role count was 1 and `create_sent=false`; no `cmd=16` occurred in this connection.
- Server source `server/truthan_local_server_v026.py` reads `TRUTHAN_DATA_SPAWN` at line 274 and emits the gated fixtures through `data_spawn_entities()` at lines 810-826. The two exact fixture frames and runtime rows are behavioral evidence that the unchanged PID had the data-spawn path active. A direct environment read was unavailable: CIM returned Access Denied and Python `psutil` is not installed.
- Redacted screenshots: `runtime/agent/ui_control/20260925T122649_345999Z/009.png` (fresh in-game view) and `runtime/agent/ui_control/20260925T123303_814068Z/001.png` (world HUD after disconnect). The first screenshot shows the player, but neither test fixture is clearly identifiable in the captured viewport. **Visual appearance is not verified.** Packet delivery/runtime rows are not visual proof.
- The game socket closed at 21:33:05 JST with `server_idle_300s`. The first `move --duration-ms 600` was rejected before input because its observation expired at the final freshness guard. After adding the regression-covered refresh path, the next attempt stopped at the current-connection/cmd10 gate because the five-minute idle close had occurred. No movement input was sent. No second app restart/login was attempted; the one app-only restart already succeeded, and the requested login retry limit was respected.

## Commands and results

- `git status --short`, `git diff --stat`, `git log --oneline -10`: inspected at start; HEAD was `8164e00`, with only the two UI-controller files modified.
- `py -m unittest tests.test_truthan_ui_control`: 16 tests passed after the movement freshness regression was added.
- `py tools/truthan_ui_control.py bootstrap --restart-client`: exit 0; reached `IN_GAME`; reused the existing role; both cmd132 fixture frames observed.
- `py tools/truthan_ui_control.py move --duration-ms 600`: first blocked by observation expiration before input; later blocked because the current game connection had timed out. No movement pass claimed.
- `git diff --check`: passed; only Git line-ending conversion warnings were reported.

## Status and next action

Real-client bootstrap and cmd132 delivery for both test fixtures: **PARTIAL**. Visual appearance of either fixture: **UNKNOWN**. Current blocker is the server's five-minute idle socket close before the visual inspection movement could be made. In a follow-up run, reconnect to the same in-memory server/role and move immediately after bootstrap, then capture a redacted screenshot focused on both fixture positions. Do not create another role.
